<?php

namespace App;

// use Illuminate\Database\Eloquent\Model; // commit it because of Model.php(Parent Class)

class Post extends Model {

	// protected $fillable = [ 'title', 'body' ];
	// protected $guarded = [ 'id' ];
	// protected $guarded = [];
    
}
